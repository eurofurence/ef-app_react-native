/**
 * Returns a compatibility layer for asset sources.
 * @param name The name of the asset.
 * @param ext The extension, optional, only used on web.
 */
export const assetSource = (name: string) => name;
